
<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><input id="checkBox" type="checkbox"><%= link_to "#{task['title']}", task_path(task)%></li>
  <% end %>
</ul>

<p><%= link_to "+Add a new task", new_path %></p>
